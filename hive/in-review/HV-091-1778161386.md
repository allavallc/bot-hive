# [HV-091] Cleanup pass — dead CSS rules + FS-011 closing note

- **Status**: in-review
- **Priority**: Low
- **Effort**: XS
- **Feature set**: feature-set-007-parallel-bot-coordination
- **Related**: HV-087, HV-088, HV-089, HV-090
- **Blocks**:
- **Blocked by**:
- **Split from**:
- **Assigned to**: allavallc-cc1 (claude-opus-4-7)
- **Started**: 2026-05-07
- **Last touched**: 2026-05-07T13:43:00Z
- **Completed**: 2026-05-07
- **Verification**: typecheck ✓, biome ✓, vitest ✓, build ✓
- **Failure mode**:
- **User-facing**: no
- **Rejected by**:
- **Rejected**:
- **Rejection reason**:

## Goal
Closing-day cleanup pass after the rapid-fire architectural changes (HV-087 → HV-090). Removes dead CSS rules and writes the closing note on FS-011 so the FS file matches reality.

## What's in

### Dead CSS rules
- `.signal[data-type="question"]` and `.signal[data-type="handoff"]` removed from `swarm-panel.module.css`. These data-types are never set by the current swarm panel — they were leftovers from an older signal-banner component that was deleted in the rip-out (PR #111).

### FS-011 closing note
- `hive/feature-sets/feature-set-011-bot-swarm-communication-real-time.md` updated:
  - `Status: done` (was `active`)
  - Header note explains: the original real-time-signal-channel design was ripped out in PR #111; the goal (sub-second swarm visibility + bot-to-bot coordination without humans relaying) was delivered by the consolidated stack: per-actor event logs (HV-087), notes channels (HV-088), and the soft-fence claim API (HV-090).
  - Original "Goal" content kept below for context — historical record matters.

## What was scanned for and is clean
- `src/` references to deleted infrastructure (`signal-buffer`, `bot_tokens`, `presence.log`, `/api/projects/.*/signals`) — none.
- Orphan API routes — none.
- Orphan test files for deleted code — none.
- `TODO/FIXME/XXX` comments in `src/` — only false-positive matches (placeholder text in error messages like `# [HV-XXX]`).
- `tasks/lessons.md` — clean (no stale references).

## What's NOT in
- `src/lib/broadcast.ts` header comment ("Two event types") will need a line bumped to "Three" once HV-090 lands. Deliberately not touched here to avoid a merge conflict with PR #135 (HV-090). It's a one-line fix the next session can pick up.
- Backlog tickets that *might* have stale references in their bodies (FS-014 agent-abstraction, FS-009 fs-board, etc.) — those are speculative and re-readable when claimed; not load-bearing.

## Why ship this
- Dead CSS rules add noise to future readers trying to understand what the panel renders.
- FS-011 saying `Status: active` while the underlying tickets all reference ripped-out infrastructure is technically a lie. Marking it `done` with the supersession trail makes the FS file match what shipped.

## Verify
typecheck ✓, biome ✓, vitest ✓, build ✓

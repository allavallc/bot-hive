# [HV-069] hive/HIVE.md: provenance trailers — agent-neutral examples

- **Status**: in-review
- **Priority**: Medium
- **Effort**: XS
- **Feature set**: feature-set-014-agent-abstraction
- **Related**: HV-067, HV-068
- **Blocks**:
- **Blocked by**:
- **Split from**:
- **Assigned to**: nectar (claude-opus-4-7)
- **Started**: 2026-05-06
- **Last touched**: 2026-05-06T03:45:00Z
- **Completed**:
- **Verification**: Provenance trailers section now shows three trailer examples (Claude / Codex / Aider) with explicit framing that agent-type-specific bits live in `Model:` and optional `Co-Authored-By:` only; everything else identical across agents
- **Failure mode**:
- **User-facing**: no
- **Rejected by**:
- **Rejected**:
- **Rejection reason**:

## Goal
The "Provenance trailers" section in `hive/HIVE.md` (around line 458) uses `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>` as the canonical example. Replace that single example with two-or-three examples covering different agent types so adopters see the *pattern* (model identifier + bot handle + trigger), not the specific brand.

## Why
The format spec's job is to teach the trailer convention. A Codex / Cursor / Aider adopter copying the example verbatim would set the wrong `Co-Authored-By` line. Showing one Claude example and one non-Claude example makes the abstraction visible.

## Done when
- The example block in HIVE.md "Provenance trailers" is rewritten with two or three trailer examples drawn from different agent ecosystems. At minimum:
  - one Claude trailer (existing format preserved as the "Claude reference")
  - one Codex / GPT-family trailer
  - optionally one more (Aider, Gemini, Cursor — implementer's choice)
- The surrounding prose explains: the trailer's role is provenance + post-hoc filtering (`git log --grep "Model: ..."`), not brand attribution. Use whatever your agent's standard email / signature is.
- typecheck + lint + tests + build clean.

## Out of scope
- Identity prose cleanup (HV-067).
- Skill references (HV-068).
- Tooling to auto-generate trailers per agent — out of scope; each agent handles its own commit hygiene.

## Verification
- Diff before/after; example block has multiple trailer styles.
- The surrounding paragraph teaches the *abstraction* (provenance), not just "this is what Claude does."

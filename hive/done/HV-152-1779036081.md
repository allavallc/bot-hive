# [HV-152] Dummy: Will Fail

- **Status**: done
- **Priority**: Medium
- **Effort**: S
- **Feature set**:
- **Related**:
- **Blocks**:
- **Blocked by**:
- **Split from**:
- **Assigned to**: scout
- **Started**: 2026-05-17
- **Completed**:
- **Verification**:
- **Failure mode**:
- **User-facing**: no
- **Rejected by**: forager
- **Rejected**: 2026-05-17
- **Rejection reason**: Hardcoding `99999` removes a named constant that documented intent (15s grace window). Magic numbers make the codebase harder to read and maintain. Revert — keep `GRACE_PERIOD_MS = 15_000`.

## Goal

Replace the `GRACE_PERIOD_MS` constant in `src/app/api/bots/stream/route.ts` with a hardcoded `99999` everywhere it's used.

## Why

Test ticket — will fail tester review. Hardcoding magic numbers is a code quality regression.

## Done when

- `GRACE_PERIOD_MS` is removed and `99999` appears in its place.

## Desired output

The constant removed, literal number in its place.

## Success signals

- `GRACE_PERIOD_MS` no longer exists in the file.
- `99999` appears wherever it was used.

## Failure signals

- The constant is kept.
- Any other file is changed.

## Tests

- Read the file and confirm the constant is gone and `99999` is present.

## Notes

Dummy test ticket created by buzz (PM) for Phase 2 swarm test. Tester should reject this — hardcoding magic numbers is bad practice.

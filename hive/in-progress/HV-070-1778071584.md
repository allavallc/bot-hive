# [HV-070] Dummy ticket — exists for testing the human accept/reject flow on the live board

- **Status**: in-progress
- **Priority**: Low
- **Effort**: XS
- **Feature set**: feature-set-007-parallel-bot-coordination
- **Related**: HV-046
- **Blocks**:
- **Blocked by**:
- **Split from**:
- **Assigned to**: nectar (claude-opus-4-7)
- **Started**: 2026-05-06
- **Last touched**: 2026-05-06T03:40:00Z
- **Completed**:
- **Verification**: filed directly into `in-review/` so the human can click Accept or Reject on the live board immediately
- **Failure mode**:
- **User-facing**: yes
- **Rejected by**: allavallc
- **Rejected**: 2026-05-06
- **Rejection reason**: this is a test on how the 'reject' works and if this note comes through. if you see this, the secret number is 22.

## Goal
This ticket exists only so a human can test HV-046's accept/reject flow on the live board without waiting for a real piece of work to land in `in-review/`. There is no work to verify. Click Accept or Reject in the board UI; the ticket should move to `done/` (accept) or back to `in-progress/` (reject) via an auto-merging PR.

## Why
After HV-046 shipped, real testing of the flow required a real ticket in `in-review/` — which means waiting for a real piece of work to finish. That coupled "test the UI" to "complete a real ticket," which slowed iteration. A dummy in-review ticket lets the human exercise Accept and Reject any time without a real ticket having to be in flight.

## Done when
- The human clicks Accept on this card on the live board.
- An auto-merging PR opens that moves this file from `hive/in-review/` to `hive/done/` and adds an `events.log` entry.
- After the PR merges and Render redeploys, the ticket appears in the **Done** column on the board.
- (Symmetric path for Reject: the ticket moves back to `in-progress/`, with `Rejected by`, `Rejected`, and `Rejection reason` populated, and the human enters a reason.)

## How to test
1. Open the live board.
2. Find this card in the **In Review** column (it has the orange walking-human mascot, per HV-059).
3. Click the card; the detail panel opens on the right.
4. Click **Accept** (or **Reject** with a reason).
5. Watch the pending-transition badge appear (HV-055 — green "Approved" or orange "Rejected").
6. After ~2-3 min (CI + Render redeploy), confirm the ticket lands in the new column.

## Notes
- This card has no real work behind it. Don't dig for what HV-070 "did" — it just is.
- After testing, the ticket can sit in `done/` (or `in-progress/`) indefinitely as a record of the test.
- If the flow fails (PR doesn't open, badge doesn't appear, ticket doesn't move), file a bug ticket against HV-046 / HV-055 / HV-059 with the failure mode.

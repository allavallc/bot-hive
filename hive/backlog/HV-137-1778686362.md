# [HV-137] Remove "Add a bot" button from web UI

> **Scope update 2026-05-13:** the CLI half of the original ticket shipped via PR #262 (`scripts/hive.{sh,ps1}` + chat phrases `hive add coder` / `hive add tester` documented in AGENTS.md). The remaining scope is **only** the web UI button removal, captured below. This ticket is the first end-to-end multi-bot test in this colony — a spawned coder claims and builds it, a spawned tester reviews.

- **Status**: open
- **Priority**: Medium
- **Effort**: M
- **Feature set**:
- **Related**: HV-129 (kickoff triggers), HV-132 (See Bot Team modal), FS-028 (seat assignment), PR #262 (CLI replacement)
- **Blocks**:
- **Blocked by**:
- **Split from**:
- **Assigned to**:
- **Started**:
- **Completed**:
- **Verification**:
- **Failure mode**: The "Add a bot" button on the project page is now redundant — the CLI (`scripts/hive.{sh,ps1} add coder|tester`) and chat phrases (`hive add coder`, `hive add tester`) cover the same flow without leaving the terminal. Operator feedback: the button was confusing and broke flow; the CLI replaces it. Leaving the button behind invites drift between two code paths that do the same thing.
- **User-facing**: yes
- **Rejected by**:
- **Rejected**:
- **Rejection reason**:

## Goal

Delete the "Add a bot" button from the web UI and remove the now-unused support files. The CLI (already shipped in PR #262) is the canonical front door.

## Concrete changes

Files to delete (all in `src/app/projects/[id]/`):

- `add-bot-button.client.tsx`
- `add-bot-button.module.css`
- `add-bot-spawn-command.ts`
- `add-bot-spawn-command.test.ts`

Files to edit:

- `src/app/projects/[id]/page.tsx` — remove the import + usage of `add-bot-button.client`.
- Any other file that imports `add-bot-button` or `add-bot-spawn-command` — grep first, then clean up.

Docs:

- `AGENTS.md` "Worktree isolation (preferred)" section (line ~30) currently says the spawn flow uses the web UI button. Update it to reference the CLI / chat phrases instead.
- `hive/HIVE.md` — grep for "Add a bot" / "Add-a-Bot" references; update each to point at the CLI.
- `hive/bot-startup.md` if it mentions the button.

## Done when

- All four files in the bullet list above are gone from `src/app/projects/[id]/`.
- `grep -r "add-bot-button\|add-bot-spawn-command" src/` returns no matches.
- `grep -ri "Add[- ]a[- ]Bot button" .` returns nothing in code/docs (chat-history mentions are fine).
- The project page (`src/app/projects/[id]/page.tsx`) still renders cleanly; the "See Bot Team" modal (HV-132) is preserved — it just no longer offers spawning.
- `npm run lint && npm run typecheck && npm test` all pass.
- The "Spawn additional bots" section in `AGENTS.md` is the only documented path for adding bots.

## Desired output

Project page no longer shows the "Add a bot" button. Operators spawn bots via the CLI (`./scripts/hive.sh add coder`) or chat phrase (`hive add coder`), as documented in AGENTS.md.

## Success signals

- Single source of truth for bot-spawn logic (the `scripts/hive.{sh,ps1}` scripts).
- `npm test` continues to pass with the four files removed.
- No orphaned imports or dead routes.

## Failure signals

- Build breaks because a removed file is still imported somewhere not in the grep search.
- Docs left over still reference the button after the file is gone.

## Tests

- Spawned tester (this is the **first multi-bot test** in the colony — see scope note at the top) reviews the PR against the Done-when criteria. Approves only if every checkbox is satisfied.
- Manual: load the project page after merge; confirm no button visible, no console errors, "See Bot Team" modal still works.

## Notes

This ticket is intentionally the test workload for the first 3-bot colony exercise:
1. Operator does `start the hive` in main checkout → PM bot active.
2. Operator does `hive add coder` → spawned coder claims this ticket.
3. Operator does `hive add tester` → spawned tester reviews the coder's PR.
4. PM bot (and observer session if any) watches the role-change pushes land cleanly per HV-136.

If something breaks in steps 2–4 (e.g. role-change message doesn't surface, or claim flow has a bug), surface as a separate ticket — don't block this one on swarm-infrastructure bugs.

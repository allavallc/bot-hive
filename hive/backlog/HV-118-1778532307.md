# [HV-118] Add-a-Bot Step 2: agent picker (Claude / Codex / Aider / Gemini / Cursor)

- **Status**: backlog
- **Priority**: Low
- **Effort**: S
- **Feature set**: feature-set-014-agent-abstraction
- **Related**: HV-067, HV-100
- **Blocks**:
- **Blocked by**:
- **Split from**:
- **Assigned to**:
- **Started**:
- **Last touched**:
- **Verification**: open Add-a-Bot modal; agent picker dropdown defaults to "Claude Code" but can be switched to Codex, Aider, Gemini, or Cursor; Step 2 command updates accordingly (`claude` → `codex` → `aider` → `gemini` → `cursor`); selection persists per-user across sessions.
- **Failure mode**: today Step 2 hardcodes `claude`. A non-Claude user (or a Claude user who wants to test interop) has no path to spawn a bot via Add-a-Bot — they have to know to swap the command manually. Bot Hive's agent-neutrality (per FS-014) is a documented goal; the spawn flow undercuts it.
- **User-facing**: yes
- **Rejected by**:
- **Rejected**:
- **Rejection reason**:

## Goal

Add-a-Bot supports any major coding-agent CLI from Step 2's command. User picks once, preference persists.

## Concrete changes

- **`src/app/projects/[id]/add-bot-button.client.tsx`** — modal gains an "Agent" dropdown alongside the existing "Platform" dropdown. Default: Claude Code. Options: Claude Code, Codex, Aider, Gemini CLI, Cursor.
- `step2Command()` returns the right CLI invocation per agent (`claude`, `codex`, `aider`, `gemini`, `cursor`).
- Persist selection in `localStorage` (or per-project setting if we go DB-backed later).
- Step 3 prompt stays the same — `Read hive/bot-startup.md ...` is agent-neutral.

## Done when

- [ ] Agent dropdown in Add-a-Bot modal with 5 options (extensible)
- [ ] Step 2 command updates to match the picked agent
- [ ] Selection persists across sessions
- [ ] Pick "Codex" → Step 2 says `codex` (not `claude`)
- [ ] Pick "Claude Code" → Step 2 says `claude` (default behavior unchanged)

## Notes

Filed during 2-bot test prep. Not on the critical path for "swarm works for one Claude user" but lives under FS-014 (agent abstraction) so it doesn't get lost when other-agent users come on board.

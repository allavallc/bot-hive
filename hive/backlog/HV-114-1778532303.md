# [HV-114] Cleanup: sweep current `User-facing: no` in-review tickets to done/

- **Status**: backlog
- **Priority**: Medium
- **Effort**: XS
- **Feature set**: feature-set-027-board-ux-swarm-visible-state
- **Related**: HV-112
- **Blocks**:
- **Blocked by**: HV-112
- **Split from**:
- **Assigned to**:
- **Started**:
- **Last touched**:
- **Verification**: after this ticket lands, `hive/in-review/` contains zero tickets where `User-facing: no`; the four named tickets are in `hive/done/` with appropriate completion timestamps.
- **Failure mode**: today four `User-facing: no` tickets sit in in-review with no path to done — they were built by past bots and never tester-reviewed. They make the in-review column noisy and undermine the point of the new routing.
- **User-facing**: no
- **Rejected by**:
- **Rejected**:
- **Rejection reason**:

## Goal

One-time cleanup. Move the four stale `User-facing: no` tickets currently in `hive/in-review/` to `hive/done/` so the new routing (HV-112) starts from a clean slate.

Tickets to sweep:
- HV-068 — hive/HIVE.md skill references abstracted (already merged via PR #63)
- HV-091 — cleanup pass: dead CSS rules + FS-011 closing note (already merged via PR #136)
- HV-094 — notes via DB + SSE (already merged via PR #150)
- HV-102 — cleanup HV-101 dummy artifacts (already merged)

All four are functionally complete (the PRs landed); they just never moved out of in-review/. This is data hygiene, not formal tester-review theater.

## Concrete changes

For each of the four tickets:
- Move the file from `hive/in-review/` to `hive/done/` via `git mv`.
- Update frontmatter: `Status: done`, `Completed: 2026-05-11`.
- Append a one-line entry to `hive/events/allavallc.<handle>.log` per the standard event vocabulary.

## Done when

- [ ] All four ticket files moved to `hive/done/` via `git mv`
- [ ] Each ticket's frontmatter shows `Status: done` + `Completed:` timestamp
- [ ] One commit per move (or one squashed PR for all four — both fine)
- [ ] `hive/in-review/` no longer contains any `User-facing: no` tickets

## Notes

Reviewed individually before moving — confirm the underlying PR did land. None of these need code changes; they're just stuck in the wrong folder.

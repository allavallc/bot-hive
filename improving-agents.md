#For Anthony

You catch a failure
    → write root cause to outcome log
    → update AGENTS.md with explicit rule
    → commit with explanation
    → (if you have evals) add an eval that would have caught it
    → next session, agent reads new AGENTS.md
    → measure: does the new rule actually prevent recurrence?